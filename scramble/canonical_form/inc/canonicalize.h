#define MAX_LEN_CANONICAL_STR 32
#define MAX_LEN_STR 32
int
canonicalize(
    const char * const in_str, // input 
    uint8_t cstr[MAX_LEN_CANONICAL_STR]
    )
{
  int status = 0;
  uint8_t count[NUM_ALPAHBET];
BYE:
  return status;
}
